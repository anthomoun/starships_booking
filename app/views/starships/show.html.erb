<div class="banner">
  <div class="container">
    <h1><%= @starship.name %></h1>
  </div>
</div>

<div class="container">
  <div class="row mt-5">
    <% @starships.each do |starship| %>
      <div class="col-4">
        <div class="card-category">
          <%= starship.name %>
        </div>
      </div>
    <% end %>
  </div>
  <div class="row my-5 justify-content-center">
    <div class="col-4">
    </div>
  </div>
</div>
